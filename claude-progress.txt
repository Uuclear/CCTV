# Shift log — CCTV-report

## 2026-05-06（卡住修复 & E2E）

- **卡住原因**：`npm install` 默认触发 Playwright 浏览器下载，弱网下极慢或超时。
  - 处理：`init.ps1` / `init.sh` 设置 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`；首次 E2E 用 [`scripts/e2e.ps1`](scripts/e2e.ps1) 单独 `playwright install chromium`（可重试）。
- **依赖**：[`backend/requirements.txt`](backend/requirements.txt) 已列入 `python-docx`、`docxtpl`、`rapidocr-onnxruntime`，避免新 venv 缺包。
- **E2E**：[`frontend/playwright.config.ts`](frontend/playwright.config.ts) 自动拉起 uvicorn + `vite dev`；[`frontend/e2e/smoke.spec.ts`](frontend/e2e/smoke.spec.ts) 已跑通。
- **报告/PDF**：`POST .../reports/docx|pdf`；PDF 无 LibreOffice 时 503；[`desktop/run-dev.ps1`](desktop/run-dev.ps1) 双进程开发。
- **Tauri**：已在 `frontend/src-tauri` 集成 Tauri 2；`npm run tauri:dev` 经 `scripts/tauri-dev-bootstrap.mjs` 拉起后端+Vite；图标由 `npx tauri icon` 自 1024 PNG 生成。`feature_list` 中该项已 `true`。
- **模板与委托单**：已新增 [`templates/commission_fields.md`](templates/commission_fields.md)、[`templates/report/docxtpl-context.md`](templates/report/docxtpl-context.md)；报告渲染统一 `build_report_context`；仓库内生成 [`templates/report/jinja_minimal.docx`](templates/report/jinja_minimal.docx) 供默认导出。
- **标准数字化（进行中）**：`config/standards/db31t444-2022/rules.yaml` 与 CJJ 181 对齐骨架见前文提交；表与系数须对照 DB31 正式 PDF 终审。
- **Linux 常见报错**：`could not find openssl development headers` → 按 [`docs/TAURI.md`](docs/TAURI.md) 安装 `libssl-dev`、`pkg-config` 与 WebKit/GTK 依赖后再编译。若 `npm run tauri:dev` 提示找不到 venv 内 Python，已支持依次尝试 `.venv/bin/python` 与 `.venv/bin/python3`。

## 2026-05-06

- 初始 harness + FastAPI/React 骨架、视频抽帧等见此前提交。
